# Copy of chatbot-backend/.env.example for project-root setup.
# The backend loads both this file and chatbot-backend/.env.

ENVIRONMENT=development
ALLOWED_ORIGINS=http://127.0.0.1:5500,http://localhost:5500

# Optional OpenAI phrasing layer. Local retrieval still works without a key.
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
